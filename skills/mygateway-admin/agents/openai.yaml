interface:
  display_name: "MyGateway Admin"
  short_description: "Manage MyGateway resources and diagnostics"
  default_prompt: "Use $mygateway-admin to load the saved connection, check the hosted skill version, then inspect or manage the MyGateway deployment."
