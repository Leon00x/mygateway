interface:
  display_name: "MyGateway Admin"
  short_description: "Inspect and manage a MyGateway deployment"
  default_prompt: "Use $mygateway-admin to connect, summarize the gateway's setup state, and guide me through the next useful action."
